import {
    StyledHeading
} from "./h1";
import {
    StyledHeading2
} from "./h2";
import {
    StyledLink
} from "./a";
import {
    StyledList,
    StyledListItem
} from "./list";
import {
    StyledNextButton
} from "./nextbutton";

export {
    StyledHeading,
    StyledHeading2,
    StyledLink,
    StyledList,
    StyledListItem,
    StyledNextButton
}
